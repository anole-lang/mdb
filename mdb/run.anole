# Parse the `book.toml`
# Load the book chapters described by `SUMMARY.md` into memory
# Preprocess all loaded book chapters
# Generate the final static pages for the book

use env;
use read_dir from os;

@root: env.args()[1];
